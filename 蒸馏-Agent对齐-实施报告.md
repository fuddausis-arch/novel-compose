# 蒸馏体系重构 · 实施报告

> 项目：NovelCompose（织谱）多 Agent AI 小说创作平台
> 日期：2026-08-12
> 范围：蒸馏（skill 提取）→ 标签 → 注入 全链路改造

---

## 一、一句话结论

把"蒸馏出来的技能"和"35 个写作 Agent 的分工"对齐了：**每个 Agent 现在只拿到自己负责那一块的技能**，不再把所有技能一刀切塞给所有 Agent。既提高了生成质量，又防止几十本书蒸馏后上下文爆炸。

---

## 二、为什么要做（背景）

1. **质量问题**：项目有 35 个分工明确的 Agent（写对话的、写动作的、写内心的、建世界观的…），但之前蒸馏技能注入是"一刀切"——所有写手拿到同一份技能。写对话的 Agent 看不到对话精华，写动作的看不到动作精华，等于把精读原文的成果浪费了。
2. **上下文爆炸**：9 本书 × 每本几百个碎片 = 上千个 Skill。规则型技能全量注入会撑爆上下文，书越多越严重。
3. **结构混乱**：二级浓缩是"整本书 1 个总纲"，没有按维度归类，Agent 不知道该看哪份。

---

## 三、做了什么（6 大项）

### 1. 蒸馏维度：15 个 → 19 个

保留原有 15 个（老数据不坏），新增 4 个，正好对应 4 个专项写手：

| 新维度 | 对应 Agent |
|---|---|
| 16 动作与打斗 | 动作写手（novel-action-writer）|
| 17 内心与心理 | 内心写手（novel-internal-writer）|
| 18 环境与描写 | 描写写手（novel-description-writer）|
| 19 过渡与节奏 | 过渡写手（novel-transition-writer）|

### 2. 每个维度"蒸馏什么"是确定的（专属模板）

8 个重要维度用专属输出模板，字段固定，不允许随便蒸馏：

| 维度 | 模板要求必须拆出 |
|---|---|
| 世界观与设定 | 力量体系规则、设定投放方式、专属术语、一致性维护 |
| 爽点设计 | 爽点类型、铺垫→释放模式、爽点密度 |
| 章节钩子 | 断章钩子类型、钩子位置频率、开篇抓人手法 |
| 对话与台词 | 对话占比、角色语言标志、潜台词写法 |
| 动作与打斗 | 打斗节奏节拍、动作描写粒度、力量感、紧张感 |
| 内心与心理 | 内心独白风格、情绪层次、内心冲突、揭示节奏 |
| 环境与描写 | 氛围营造、感官细节、借景叙事、描写克制 |
| 过渡与节奏 | 过渡方式、衔接技巧、节奏换挡、连续性 |

其余 11 个维度用通用模板。**另加一条铁律：每个技能的黑名单（禁止写法）必须 ≥6 条**——最锐利的方法论藏在"原作者不会怎么写"里。

### 3. 三级分层体系（对应你说的：三级=说明书、二级=规格、一级=素材库）

| 层级 | 数量 | 内容 | 怎么用 |
|---|---|---|---|
| 一级 · 素材库 | 几百个碎片（片段×维度） | 每段原文 × 每个维度的分析 | 写的时候按上下文检索，命中哪段用哪段 |
| 二级 · 规格 | 每本书 19 个 | 每个维度 1 份浓缩总纲 | 按 Agent 定向注入（谁负责哪块就给谁）|
| 三级 · 说明书 | 每本书 1 本 | 全部二级再浓缩的精华 | 所有 Agent 必看，总览全书风格 |

**本次新增**：三级说明书里带一张"维度索引表"——列出本书每个维度对应写作的哪个环节、写到哪块该翻哪份二级总纲。比如"写高潮章/打脸场景时 → 翻『爽点设计』总纲"。

### 4. Skill 打标签（归属 Agent）

**只有定向注入的技能打标签**：二级维度总纲（按维度打对应 Agent）和三级说明书（全部 Agent 必看）。
**一级碎片（素材库）不打标签**——它是按内容检索的（写战斗就检索战斗素材），要能跨维度命中，不按 Agent 归属。每个技能文件写入 `agent_roles` 标签，标明它归谁用。

### 5. 注入按 Agent 过滤（核心）

- **生成时**：每个 Agent 只注入【通用技能 + 自己维度的技能】。世界观写手只拿"世界观"总纲，对话写手只拿"对话"总纲，不再全塞。
- **兼容性**：2167 个旧技能没有标签，按"通用"处理照常注入，老数据零影响。

### 6. 前端蒸馏页

维度列表更新为 19 项，可直接勾选新维度。

---

## 四、改动文件清单

| 文件 | 改动 |
|---|---|
| novel_agent/distillation/engine.py | 维度 15→19、8 个专属模板、黑名单≥6、二级按维度分组浓缩、三级维度索引、agent_roles 标签、碎片写 dimension 维度号 |
| novel_agent/api/routes_skills.py | 注入按 agent_roles 过滤；检索按维度加权（同维度加分、跨维度不丢）；向量层限流修复（节流+退避+本地 bge 降级）|
| novel_agent/workflows/loader.py | 工作流注入：写手系+维度覆盖的 Agent 按自身维度定向注入 |
| novel_agent/api/routes_distillation.py | 注释更新（维度范围 1-19）|
| frontend/src/pages/settings/DistillationPage.tsx | 维度列表 15→19 |

---

## 五、验证结果

| 项目 | 结果 |
|---|---|
| 后端语法检查（py_compile × 4 文件） | ✅ 通过 |
| 前端类型检查（tsc --noEmit）| ✅ 通过 |
| 注入过滤单元测试 | ✅ 世界观写手拿到世界观总纲；写手拿不到；旧数据全放行 |
| 二级分组浓缩测试 | ✅ 按维度产出 3 个总纲（_d1/_d9/_d16 后缀）|
| 三级维度索引测试 | ✅ 三级含索引表、二级不含 |
| 检索维度加权测试 | ✅ 动作写手检索时动作碎片 3.0 分 > 对话写手 2.0 分；跨维度不丢 |
| 限流修复实测 | ✅ 429 触发自动降级/退避重试路径（见第五节补充）|
| 后端重启加载新代码 | ✅ API 正常 |
| 残留任务清理 | ✅ 3 本卡住的蒸馏任务已停止 |
| 测试污染文件清理 | ✅ 已全部删除 |

### 五-补充：检索与限流问题排查（2026-08-12 第二轮）

**发现问题（实测）**：
1. 检索不准确：查询"第5章 剑冢试炼 打脸"捞到的是别的书开头的碎片——查询词太短 + 碎片粒度粗。
2. 向量层形同虚设：方舟 embedding 接口持续 429（`AccountRateLimitExceeded`），系统退回纯关键词。
3. 本地兜底失效：`sentence_transformers` 未安装，429 后无法降级本地模型。

**已修复**：
1. **碎片加维度号标签（dimension 字段）**：检索时当前 Agent 负责的维度命中 +1 分（同维度素材排前），跨维度素材不丢（加权不过滤）。
2. **向量层限流修复**：批量 upsert 节流（1.2s/批）+ 429 退避重试（8s）+ 自动降级本地 bge 模型（离线无限流，一劳永逸）。
3. **安装 sentence_transformers 5.7.0**（清华镜像）：本地 bge 兜底依赖就绪。实测向量层成功启用（80s 全量同步，不再 429 失联）。
4. **更换 Embedding API Key（根因修复）**：.env 里旧的 ARK_EMBEDDING_API_KEY 触发 429（配额/权限问题），按用户提供的新 Key 更新（`ark-f8471618-...`）。新 Key 直连测试成功（2048 维），重启后向量同步 3.1s 完成、无 429。模型固定 `doubao-embedding-vision-251215`（按方舟文档建议不混用版本）。

**遗留**：
- 查询词仍较短（"第X章+标题+类型"），建议后续把章节概要拼进查询词（本轮未做，待确认）。
- 旧碎片无 dimension 标签，加权只对新蒸馏的碎片生效（不影响正确性）。

### 五-补充2：旧碎片标签迁移 + 全方位覆盖检查（2026-08-12 第三轮）

**旧碎片迁移（已完成）**：
- 2167 个旧碎片从文件名反推标签并写回：`dimension`（维度号，r{N}）+ `work_id` + `work_title`（书名，查蒸馏 DB）
- 纯本地文件操作，不重新蒸馏、不调模型；已带标签的新文件跳过
- 实测：novel-writer 检索时爽点维度(r9)碎片 3.0 → 4.0 分（+1），加权生效；向量层正常

**全方位覆盖检查结论**：

| 环节 | 状态 |
|---|---|
| 蒸馏引擎（19 维度/专属模板/黑名单≥6/二级分组/三级索引）| ✅ |
| 注入过滤（正式写作 agents 工作流按 agent_type 定向）| ✅ |
| 检索加权（with_context 传 agent_type，同维度+1、跨维度不丢）| ✅ |
| 向量层（新 key + 节流 + 退避 + 本地 bge 兜底）| ✅ |
| 旧碎片标签（dimension + 书标签）| ✅ |
| 前端（蒸馏页 19 维度）| ✅ |
| 老数据兼容（无标签=通用）| ✅ |

**已知缺口（未来风险，当前不爆）**：
1. 交互式创作 / 书级批量生成路径（interactive_chat_stream / orchestrator BookRunner）的规则型注入不做 agent 过滤（章节级公共注入）——当前二级总纲数量少，不构成问题；将来蒸馏书多起来（几十本 × 19 总纲）需按 agent/书单过滤。
2. 查询词偏短，建议拼入章节概要（见"遗留"）。

### 五-补充3：已知缺口修复 + 旧标签补全（2026-08-12 第四轮）

**缺口1（注入防爆）已修复**：规则型注入加总量上限（60000 字符）+ 优先级保护——
- 优先级：内置/自建 > 三级说明书 > 融合总纲 > 二级维度总纲
- 超限时按优先级截断（核心约束与说明书必注入），防几十本书总纲塞爆上下文
- **未超限时全量注入，行为与旧版完全一致**（实测当前注入 15908 字符 < 上限，不触发截断，零质量影响）
- 单测通过：超限场景内置+三级保留、二级被截断；未超限场景全量

**缺口2（查询词精准）已修复**：orchestrator 章节检索查询词加入章节概要（前 200 字）——
- 交互式创作路径原本就带 `outline_summary`，无需改
- 正式写作的 assemble_context 现在 = 章节号 + 标题 + beat 类型 + 章节概要

**旧标签补全确认**：2167 个旧碎片已全部补 `dimension` + `work_id` + `work_title`；检查确认无旧总纲文件（此前蒸馏只跑到一级），无需补。实测加权生效（r9 碎片 3.0→4.0 分）。

### 五-补充4："看全"改造（素材注入不截断）（2026-08-12 第五轮）

**背景**：用户明确"看全"原则——写小说素材必须完整或经浓缩理解后注入，禁止"截断字段、拿半截就跑"（半截素材会误导 AI）。

**改造 8 处**：

| 位置 | 改造前 | 改造后 |
|---|---|---|
| 检索注入（routes_skills） | 只给 200 字预览 | 命中后注入 `section_content` **全文** |
| 参考文件注入（nodes.py / context.py） | 硬截断"…" | 按段落边界切**完整段落块**注入 + 引导"如需完整内容请通过指令读取"，不出现"截断"标记 |
| 梗库注入（nodes.py / routes_generation.py） | 截断 | **全量注入**（"看全"才能选梗） |
| 创作意图（loader.py） | 截断 | **全量注入**（截断丢后半段创作要求） |
| 故事线识别文本（storyline/creator.py） | 硬切 | **整条目完整追加，超限停止** |
| 前文摘要/世界观（memory/core.py） | 硬切 | `_fit_full_paras`：预算内保留**尽量多的完整段落** |
| 圆桌结论（roundtable/runner.py） | 硬切 | **完整发言保留直到总量上限** |
| 向量层存储 | 只存 200 字预览 | meta 存**全文** + `vec_v:2` 版本标记（旧条目强制重灌） |

**配套修复**：方舟 embedding 单条 input 上限 100KB，超长碎片（此前 400 报错）现在统一 `_EMBED_DOC_MAX_BYTES=90000` 字节截断向量化，不再报错。

### 五-补充5：垃圾碎片清理（2026-08-12 第六轮）

**排查起因**：发现 44 万字"超大文件"，用户质疑是否真有此数据。实测确认是 **10 个 LLM 输出循环 bug 产生的垃圾碎片**（第 14/15 轮蒸馏时 LLM 无限重复"无论如何"，正常碎片仅 13KB，垃圾达 376-719KB）。

**已删除**（10 个文件，均为 `distill_w*_r14/r15.json`）：w12_c27 / w10_c31 / w11_c29 / w10_c32 / w10_c33 / w12_c29 / w12_c28 / w11_c30 / w11_c32 / w12_c29_r15。

**验证结果**：`verify_full.py` 5 项断言 **ALL PASS**（完整块切分、完整段落、参考文件引导、向量层返回全文、碎片检索不截断）。

### 五-补充6：验收工程师评估处理（2026-08-12 第七轮）

**P0（素材库"看全"缺总量护栏）→ 已修复**：
- 问题：规则型有 60K 护栏，但素材库碎片全文注入**无总量上限**——多本书 × 命中多条全文可轻松超 100K 字符，128K 窗口会爆（"看全"变"看爆"）。
- 修复：新增 `_MATERIAL_INJECT_MAX_CHARS = 40000`（routes_skills.py），素材库累计注入超限时**整条跳过**（不截断内容，符合"宁可少给、不切半截"）。与规则型 60K 护栏互补，总量 ≤ 100K 可控。
- 验证：护栏常量正确、超限跳过、正常量不误伤、语法编译通过。

**P1（定向注入仅 26-Agent 生效）→ 确认是设计边界，不改代码**：
- 现状：`nodes.py`（正式写作页）与 `routes_generation.py`（交互式创作）调注入时不传 agent_type，只有 26-Agent 工作流（loader.py）传。
- 判定：**这是正确设计**。这两条是单 Agent 路径（一个写手写全章），若启用 agent_roles 过滤，写手会拿不到其他维度的总纲（世界观/动作/对话等），反而残缺。单 Agent 全量注入（60K 兜底）+ 26-Agent 定向注入，是"完整 vs 精准"两条路径的正确分工。
- 验收口径：**定向注入以 26-Agent 工作流为准**；单 Agent 路径验证"注入完整 + 总量护栏生效"。

**P2（小问题，已知无害）**：
- 圆桌结论上限 3000 字：有意设计（总量保护），对 20+ 轮只覆盖开头几轮属实，但结论本来就是"会议精华"，不追求全文。
- bge 双份加载（archival + skills 各 ~100MB）：真，但本地模型内存成本可接受，暂不合并。
- git 29 个文件未提交：真，建议验收后统一提交。

### 五-补充7：8/11 未归档工作补录（git 大扫查发现，随蒸馏工程一并提交）

**背景**：git 提交停在 8/10 23:26，8/11 下午至 8/12 凌晨共 28 文件改动未提交。除蒸馏工程外，还发现 3 块此前未归档的工作：

**① 模型管理：思考模式开关（enable_thinking）**
- config.py LLMConfig 新增 `enable_thinking: bool | None`（None=跟随模型默认，DeepSeek 开 / 火山 coding 网关自动关；True/False 强制开关）
- routes_models.py ProviderInput/ProviderUpdate 增加该字段，前端 ModelsPage 可按供应商配置
- llm/client.py 按 `volces.com` 判断火山 coding 网关：实测请求带 thinking 参数会**服务端挂起不返回**（等满超时才超时）→ 对火山编码网关直接去掉 thinking + 采样参数（最小化请求体，可正常返回）

**② 稳定性修复三连（llm/client.py）——"蒸馏没增流/限流"根因修复**
- **代理绕过**：`trust_env=False`，不走系统/环境代理（Clash 7890）。本地代理对长等待请求会断流（TCP 半开），导致 AI 请求假死挂到超时才失败（表现为"蒸馏没增流"）
- **连接池重置**：`_reset_client()` 丢弃假死连接（keep-alive 连接 TCP 半开时 httpx 会复用导致再次挂死），下次请求新建连接
- **火山网关兼容**：见上（①）

**③ 上传/附件上限 10MB → 100MB**
- utils/file_extract.py `MAX_UPLOAD_SIZE` 与 routes_workspace.py `_MAX_ATTACHMENT_SIZE` 都放宽到 100MB（此前 10MB 传大参考书会失败）

**④ 设置页 KeepAlive 保活（frontend/src/components/keep-alive/）**
- 新增 KeepAliveRoutes 组件 + SettingsLayout 支持 props.children 保活渲染，设置页切 tab 不丢状态

**⑤ 拆书技能归类素材库**：book_to_skill.py 生成的拆书技能显式 `category: material`（内容庞大，按上下文检索注入而非全量）

---

## 六、怎么用（操作说明）

1. **蒸馏页**：选维度（可全选 19 个）→ 选级数：
   - 选 **1 次**：只出碎片（素材库，几百个）
   - 选 **2 次**：碎片 + 每维度 1 份总纲（19 份，规格）
   - 选 **3 次**：碎片 + 总纲 + 1 本说明书（含维度索引，推荐完整方案）
2. 蒸馏完成后，生成小说时各 Agent 会自动按维度取用，无需手动配置。

---

## 七、后续建议（未做，按需推进）

1. **项目"参考书单"**：将来蒸馏几十本书时，给每个项目选择参考哪些书，注入范围 = 选中书 ∩ 当前 Agent 维度。当前 100 万上下文 + 按 Agent 过滤已够用，暂缓。
2. **老数据补标签**：现有 2167 个旧技能按"通用"注入（不影响正确性），如需精确过滤可重新蒸馏或写迁移脚本。
3. **实测验收**：建议挑一本书跑 3 级蒸馏，确认二级产出 19 份总纲、三级产出 1 本带索引的说明书，再写一章对比生成质量。

---

## 八、风险与说明

- **本次改动不破坏老数据**：旧技能无标签=通用，注入行为与旧版一致。
- **非创作类 Agent（审校/世界状态等）不注入技能**，保持原有行为，避免行为漂移。
- **注入量可控**：每个 Agent 每次生成 = 通用技能 + 本维度总纲（每本书 1 份）+ 检索碎片，不会随书量线性爆炸。
